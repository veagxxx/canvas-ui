export const DPR = Math.max(window.devicePixelRatio, 2);

export const DEFAULT_STATE = {
  color: '#000000',
  lineSize: 3,
}

export const DRAW_STATE = {
  defaultLineSize: 3,
  brushLineSize: 1,
  brush: [1, 2, 3, 4],
  default: [1, 3, 5, 8]
}

